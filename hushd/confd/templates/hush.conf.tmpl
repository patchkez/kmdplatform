rpcuser={{ or (getenv "HUSH_RPC_USER") "rpcuser" }}
rpcpassword={{ or (getenv "HUSH_RPC_PASSWORD") "rpcpassword" }}
server=1
txindex=1
bind={{ or (getenv "HUSH_BIND") "127.0.0.1" }}
rpcbind={{ or (getenv "HUSH_RPC_BIND") "0.0.0.0" }}
rpcallowip={{ or (getenv "HUSH_RPC_ALLOWIP") "0.0.0.0/0" }}
