rpcuser={{ or (getenv "VRSC_RPC_USER") "assetrpc" }}
rpcpassword={{ or (getenv "VRSC_RPC_PASSWORD") "assetrpcpassword" }}
server=1
txindex=1
bind={{ or (getenv "VRSC_BIND") "127.0.0.1" }}
rpcbind={{ or (getenv "VRSC_RPC_BIND") "127.0.0.1" }}
rpcallowip={{ or (getenv "VRSC_RPC_ALLOWIP") "0.0.0.0" }}
rpcworkqueue={{ or (getenv "VRSC_RPC_WORKQUEUE") "64" }}
rpcport={{ (getenv "VRSC_RPC_PORT" ) }}
