{% for assetname, asset in items.items() %}
{% for mine in mined %}
komodod -pubkey=${pubkey} -ac_name={{ assetname }} -ac_supply={{ asset.amount 
  }} -addnode={{ seed_ip }}{% if assetname in mine %} -gen{% endif %}
      {% if 'random' in mine %}
      {% if range(0, 32767) | random % 10 == 1 %}
      -gen{% endif %}
      {% endif %}

{% endfor %}
{% endfor %}
