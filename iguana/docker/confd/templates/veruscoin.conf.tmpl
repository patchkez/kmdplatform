rpcuser={{ or (getenv "VRSC_RPC_USER") "rpcuser" }}
rpcpassword={{ or (getenv "VRSC_RPC_PASSWORD") "rpcpassword" }}
server=1
txindex=1
bind={{ or (getenv "VRSC_BIND") "127.0.0.1" }}
rpcbind={{ or (getenv "VRSC_RPC_BIND") "0.0.0.0" }}
rpcallowip={{ or (getenv "VRSC_RPC_ALLOWIP") "0.0.0.0/0" }}
